import 'author.dart';

/*class Page {
  int page;
  int perPage;
  int total;
  int totalPages;
  Author author;

  Page({this.page, this.perPage, this.total, this.totalPages});

  factory Page.fromJson(Map<String, dynamic> parsedJson) {
    return Page(
      page: parsedJson['page'],
      perPage: parsedJson['per_page'],
      total: parsedJson['total'],
      totalPages: parsedJson['total_pages'],
      author: Author.fromJson(
        parsedJson['author'],
      ),
    );
  }
}
class Author {
  String firstName;
  String lastName;

  Author({this.firstName, this.lastName});

  factory Author.fromJson(Map<String, dynamic> json) {
    return Author(firstName: Json['Mr R'], lastName: Json['Reddy']);
  }
}*/
